server:
  port: 8888

spring:
  datasource:
    url: jdbc:mysql:///user-basketball?serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
#mybatis:
#    mapper-locations: classpath:mapper/*.xml
#mybatis:
  #configuration:
  # map-underscore-to-camel-case: true  # 自动将下划线列名转为驼峰字段名


logging:
  level:
    com.ysj.blms.mapper: DEBUG